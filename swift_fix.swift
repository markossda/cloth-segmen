// BackgroundRemovalService.swift içinde bu değişikliği yap:

// ESKİ (YANLIŞ):
private let baseURL = "http://34.65.103.224:80/api/remove-background-base64"

// YENİ (DOĞRU):  
private let baseURL = "https://cloth-segmentation-api.onrender.com/api/remove-background-base64"